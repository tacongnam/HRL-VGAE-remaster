# ĐẶC TẢ CHI TIẾT CẤU TRÚC THUẬT TOÁN, AGENTS VÀ THAM SỐ THỰC THI TRONG MÃ NGUỒN VGAE-HRL-QL (v2: Multi-Objective Pareto + Temporal VGAE)

Tài liệu này tập trung mổ xẻ chi tiết các cấu trúc dữ liệu nội tại, luồng tính toán ma trận, thuật toán gradient descent, cơ chế quản lý bộ nhớ tensor và toàn bộ hệ thống tham số thực tế đang trực tiếp vận hành trong source code, tích hợp các cải tiến tối ưu hóa hiệu năng, bài toán đa mục tiêu tối ưu theo biên Pareto và cơ chế VGAE thích ứng theo thời gian.

---

## 1. Bản Đồ Tham Số Thực Thi Hệ Thống (Detailed Runtime Parameters)

Các giá trị dưới đây được trích xuất trực tiếp từ các lớp cấu hình trong `config.py` và các biến runtime của mã nguồn:

### 1.1. Thông số Không gian Trạng thái & Mạng Vật lý (`NetworkConfig`, `VGAEConfig`)
* `num_nodes = 50`: Kích thước đồ thị mạng vật lý $\vert{}V\vert{} = 50$.
* `ba_edges = 3`: Tham số Barabási-Albert mô hình hóa mạng scale-free (mỗi nút mới kết nối tới $m=3$ nút cũ).
* `function_node_ratio = 0.30`: $30\%$ nút được gắn cờ `server = True` (chính xác $\lfloor 50 \times 0.3 \rfloor = 15$ nút chức năng).
* `cpu_capacity_mips = 8000.0`: Năng lực tính toán tối đa mỗi nút (MIPS).
* `bw_min_mbps = 1000.0`, `bw_max_mbps = 10000.0`: Ngưỡng năng lực liên kết vật lý.
* `delay_min_ms = 2.0`, `delay_max_ms = 5.0`: Độ trễ truyền dẫn cơ sở trên mỗi cạnh.
* `d_in = 16`: Kích thước ma trận node features $X \in \mathbb{R}^{\vert{}V\vert{} \times 16}$. Vector 8 chiều thực tế gồm:
  1. $\text{cpu\_util} = 1.0 - c_u^{\text{free}} / c_u^{\text{total}}$
  2. $\text{cpu\_free\_norm} = c_u^{\text{free}} / \max(C)$
  3. $\text{cpu\_total\_norm} = c_u^{\text{total}} / \max(C)$
  4. $\text{is\_fn} \in \{0.0, 1.0\}$
  5. $\text{avg\_bw\_util} = \frac{1}{\vert{}\mathcal{N}(u)\vert{}}\sum_{v \in \mathcal{N}(u)} (1.0 - b_{uv}^{\text{free}} / b_{uv}^{\text{total}})$
  6. $\text{degree\_norm} = \vert{}\mathcal{N}(u)\vert{} / (\vert{}V\vert{} - 1)$
  7. $\text{proc\_delay\_norm} = d_u^{\text{proc}} / 5.0$
  8. $\text{cost\_cpu\_norm} = \text{cost}_u^{\text{cpu}} / 2.0$
  * 8 chiều còn lại được zero-padded: $\text{pad} = 16 - 8 = 8$ số `0.0`.
* `d_hidden = 64`: Kích thước đầu ra lớp GCN đầu tiên.
* `d_latent = 32`: Kích thước không gian vector tiềm ẩn cho mỗi nút.
* `d_global = 2 \times d_latent = 64`: Kích thước vector toàn cục mạng sau khi ghép `[MeanPool || MaxPool]`.
* `train_every_steps = 20`: Số SFC được xử lý giữa hai lần cập nhật gradient VGAE, thay vì train lại sau mỗi SFC.
* `neg_sample_ratio = 1.0`: Tỉ lệ negative sampling cho reconstruction loss (thay thế toàn bộ ma trận kề $N \times N$).
* `temporal = True`: Bật cơ chế GRU cập nhật `z_global` theo chuỗi thời gian cùng `topology_id`.
* `temporal_hidden = 64`: Kích thước ẩn của `GRUCell` (bằng $d_{\text{global}}$).

### 1.2. Thông số Yêu cầu Dịch vụ (`SFCConfig`)
* `arrival_rate = 20.0`, `arrival_interval = 100`: Tỷ lệ đến theo Poisson Process $\lambda_{\text{eff}} = 20 / 100 = 0.2$ requests/step.
* `vnf_len_min = 2`, `vnf_len_max = 8`: Chiều dài chuỗi VNF $F_k \in [2, 8]$.
* `cpu_req_min = 100.0`, `cpu_req_max = 500.0`: Nhu cầu CPU trên từng VNF.
* `bw_req_min = 10.0`, `bw_req_max = 100.0`: Băng thông yêu cầu trên toàn tuyến SFC.
* `deadline_min = 20`, `deadline_max = 100`: Giới hạn sống của SFC tính từ lúc xuất hiện.
* `d_sfc = 7`: Chiều vector đặc trưng SFC $\boldsymbol{\phi}(q_k)$:
  $$\boldsymbol{\phi}(q_k) = \left[s_k, d_k, b_k, F_k, (T_k^{\text{deadline}} - t), \frac{1}{\max(1, T_k^{\text{deadline}} - t)}, \sum_{i=1}^{F_k} r_i^{\text{cpu}}\right]$$
* `d_vnf = 2`: Chiều vector đặc trưng VNF $\boldsymbol{\phi}(v_i) = \left[\frac{r_i^{\text{cpu}}}{\text{cpu\_capacity\_mips}}, 1.0\right]$.

### 1.3. Thông số Chi Phí Triển Khai (`DeployCostConfig` - MỚI)
Chi phí triển khai một SFC là tổng 5 thành phần trên mỗi chặng $(n, P, r^{\text{cpu}}, b)$ trong danh sách `allocations`:
$$C_{\text{deploy}} = \sum_{\text{chặng}} \Big[ w_{\text{cpu}} \cdot \text{cost}_n^{\text{cpu}} \cdot r^{\text{cpu}} + w_{\text{ram}} \cdot \text{cost}_n^{\text{ram}} \cdot (r^{\text{cpu}} \cdot k_{\text{ram}}) + w_{\text{stor}} \cdot \text{cost}_n^{\text{stor}} \cdot c_{\text{stor}} + w_{\text{bw}} \cdot b \cdot \vert{}P\vert{} + w_{\text{init}} \cdot c_{\text{init}} \cdot \vert{}P\vert{} \Big]$$
* $k_{\text{ram}} = \text{ram\_per\_cpu\_unit} = 1.0$: Hệ số quy đổi CPU sang RAM yêu cầu.
* $c_{\text{stor}} = \text{storage\_per\_vnf} = 1.0$: Chi phí lưu trữ cố định mỗi VNF.
* $c_{\text{init}} = \text{init\_cost\_per\_hop} = 0.5$: Chi phí khởi tạo/chuyển tiếp trên mỗi hop định tuyến.
* $\vert{}P\vert{}$ là số hop (cạnh) của đường đi $P$. Với chặng đến đích cuối ($r^{\text{cpu}} = 0$), chỉ tính $w_{\text{bw}}$ và $w_{\text{init}}$.
* Tất cả trọng số $w_{\cdot} = 1.0$ mặc định, có thể hiệu chỉnh độc lập.

### 1.4. Thông số Tối Ưu Pareto (`ParetoConfig` - MỚI)
* `num_weight_bins = 11`: Số điểm rời rạc hóa trên đường trọng số $w_{\text{accept}} \in [0, 1]$, $w_{\text{cost}} = 1 - w_{\text{accept}}$.
* `chebyshev_rho = 0.05`: Hệ số điều chỉnh trong hàm scalarization Chebyshev tăng cường (augmented Chebyshev).
* `accept_weight_init = 0.5`, `cost_weight_init = 0.5`: Trọng số khởi tạo lúc train.
* `weight_adapt_rate = 0.01`: Tốc độ agent tự điều chỉnh trọng số theo tỉ lệ chấp nhận thực tế mỗi episode.
* `utopia_momentum = 0.98`: Hệ số EMA cập nhật điểm không tưởng (utopia point) cho hai mục tiêu.

### 1.5. Kích Thước Đầu Vào Mạng Cập Nhật
* `d_hl_input = d_global + d_sfc + 2 = 64 + 7 + 2 = 73`: 2 chiều bổ sung là $(w_{\text{accept}}, w_{\text{cost}})$, cho phép HL Agent trở thành **Pareto-conditioned Q-network**.
* `d_ll_input = 137`: Vector đầu vào mạng LL (giữ nguyên cấu trúc chi tiết từ bản cũ: $64 + 32 + 32 + 2 + 7 = 137$).
* `lr = 5e-4`: Tốc độ học Adam của cả hai Q-networks.
* `gamma = 0.95`: Hệ số chiết khấu tương lai $\gamma$.
* `target_update_freq = 50`: Tần suất cập nhật Hard Copy từ Q-net sang Target-net.
* `eps_start = 1.0`, `eps_end = 0.05`, `eps_decay = 0.995`.
* `hl_buffer_size = 15_000`, `ll_buffer_size = 72_000`, `batch_size = 64`.
* Tham số hàm thưởng Reward Function:
  * $\mu_{\text{cpu}} = 0.001$, $\mu_{\text{bw}} = 0.001$.
  * $\theta = 2.0$, $\lambda_L = 1.0$, $r_{\text{fail}} = 10.0$.
  * $\alpha = 1.0$, $\beta = 0.5$, $\gamma_{\text{load}} = 0.5$.
  * $\omega_{\text{bw}} = 100.0$, $\lambda_{\text{penalty}} = 1.0$.

---

## 2. Sửa Lỗi Độ Phức Tạp Thời Gian Huấn Luyện & Tối Ưu Hiệu Năng

### 2.1. Chẩn Đoán Nguyên Nhân Chậm (Bản Cũ)
1. **VGAE forward + backward toàn đồ thị sau MỖI lần commit SFC thành công**: Dựng ma trận kề $A \in \mathbb{R}^{50 \times 50}$ đặc và tính BCE trên toàn bộ $2500$ phần tử mỗi lần ($\sim 2000$ lần/episode).
2. **Tính lại `compute_load_std` sau MỖI hop** bằng vòng lặp Python thuần trên tất cả các cạnh.
3. **`custom_dijkstra` dùng lazy-deletion heap không kiểm tra khoảng cách đã lỗi thời (stale entry)**.
4. Vòng lặp Python thuần tính `path_cost`/`path_delay` trên từng cạnh.

### 2.2. Các Sửa Đổi Áp Dụng
1. **Tách chu kỳ huấn luyện VGAE**: Biến đếm `sfc_processed_since_vgae_train` chỉ kích hoạt một bước gradient VGAE mỗi khi đạt ngưỡng `cfg.vgae.train_every_steps = 20` SFC đã xử lý.
2. **Reconstruction loss dùng Negative Sampling**:
   $$\mathcal{L}_{\text{recon}} = \text{BCE}\left(\langle z_u, z_v \rangle_{(u,v) \in E}, 1\right) + \text{BCE}\left(\langle z_u, z_v \rangle_{(u,v) \sim \text{Uniform}}, 0\right)$$
   Giảm độ phức tạp từ $O(N^2)$ xuống $O(\vert{}E\vert{})$.
3. **Cache `load_std`**: `NFVEnvironment` giữ bộ nhớ đệm, chỉ tính lại khi `allocate`/`_rollback_resources` thực sự thay đổi băng thông.
4. **Dijkstra bỏ qua stale heap entries**: Thêm điều kiện `if d > dist.get(u, inf): continue` và tối ưu mũ bằng `math.exp(rho * log(omega_bw))`.
5. **Vector hóa `path_cost`/`path_delay`** bằng NumPy: Gom dữ liệu thành mảng NumPy và tính tổng bằng phép toán vector (`np.sum`).
6. **Loại bỏ comment/line-break dư thừa** trong các hot loop (`train.py`, `dijkstra.py`).

---

## 3. Bài Toán Đa Mục Tiêu: Tối Ưu Biên Pareto

### 3.1. Phát Biểu Bài Toán & Augmented Chebyshev Scalarization
Hai mục tiêu xung đột: $\max \text{AcceptanceRatio}(\pi)$ và $\min \mathbb{E}[C_{\text{deploy}}(\pi)]$. Hàm scalarization tăng cường:
$$r_H = -\left[\max_{k \in \{a, c\}} w_k \cdot (z_k^{*} - r_k) + \rho \sum_{k \in \{a, c\}} w_k \cdot (z_k^{*} - r_k)\right]$$
* $r_a$ = thưởng "chấp nhận" (revenue + urgency + LL feedback hoặc $-r_{\text{fail}}$), $r_c = -\mu_{\text{deploy}} \cdot C_{\text{deploy}}$.
* $z_a^{*}, z_c^*$ = điểm không tưởng (utopia point) cập nhật bằng EMA (`utopia_momentum = 0.98`).
* $\rho = 0.05$, $w_a + w_c = 1$.

### 3.2. Pareto-Conditioned Q-Learning & Adaptive Weights
* HL Agent nhận thêm $(w_a, w_c)$ làm đầu vào (`d_hl_input = 73`), giúp một mạng duy nhất học được toàn bộ họ chính sách dọc biên Pareto.
* Trọng số tự động thích ứng mỗi episode theo tỉ lệ chấp nhận thực tế (`weight_adapt_rate = 0.01`). Khi evaluate, `evaluate.py --pareto-scan` quét đều 11 điểm trọng số để vẽ biên Pareto (`pareto_front.json`).

---

## 4. VGAE Thích Ứng Theo Thời Gian Trên Cùng Topology

* Áp dụng `GRUCell` lên vector toàn cục:
  $$\mathbf{z}_{\text{global}}^{(t)} = \text{GRUCell}\left(\mathbf{z}_{\text{global,static}}^{(t)}, \mathbf{h}_{\text{topo}}^{(t-1)}\right)$$
* Trạng thái ẩn $\mathbf{h}_{\text{topo}}$ được lưu theo `topology_id` (băm ổn định từ $V, E$) xuyên suốt các vòng lặp môi trường có cùng cấu trúc mạng vật lý nhưng khác thứ tự request, giúp nắm bắt "pha tải" đặc trưng theo thời gian.

---

## 5. Đi Sâu Cơ Chế Hoạt Động & Toán Tử Matrix Của High-Level Agent

### 5.1. Vector Hóa Lựa Chọn Hành Động
$$\mathcal{I}_{\text{valid}} = \{i \mid q_i \in Q_t, T_i^{\text{deadline}} > t\}, \quad M = \vert{}\mathcal{I}_{\text{valid}}\vert{}$$
$$\Phi_{\text{SFC}} = \text{stack}\left(\left[\boldsymbol{\phi}(q_i)\right]_{i \in \mathcal{I}_{\text{valid}}}\right) \in \mathbb{R}^{M \times 7}, \quad Z_{\text{global}}^{\text{exp}} \in \mathbb{R}^{M \times 64}, \quad W^{\text{exp}} \in \mathbb{R}^{M \times 2}$$
$$X_{\text{HL}} = \left[Z_{\text{global}}^{\text{exp}} \,\Vert{}\, \Phi_{\text{SFC}} \,\Vert{}\, W^{\text{exp}}\right] \in \mathbb{R}^{M \times 73}, \qquad \mathbf{s} = \text{MLP}_{\text{HL}}(X_{\text{HL}}) \in \mathbb{R}^{M \times 1}$$
$$i^* = \mathcal{I}_{\text{valid}}\left[\arg\max_{k} \mathbf{s}_k\right]$$

### 5.2. Bellman Update Với Pareto Weight Cố Định Trong Một Episode
Trọng số $(w_a, w_c)$ cố định trong suốt một episode, target Bellman:
$$y_j = r_{H,j} + \gamma (1 - d_j) \mathcal{V}(S_j'), \quad \mathcal{V}(S_j') = \max_k \text{Target\_MLP}_{\text{HL}}\left(\mathbf{z}_{\text{next\_global}}^{(j)} \Vert{} \Phi_{\text{next}}^{(j)} \Vert{} \mathbf{w}^{(j)}\right)$$
với $r_{H,j}$ tính theo Chebyshev scalarization.

---

## 6. Đi Sâu Cơ Chế Hoạt Động & Toán Tử Matrix Của Low-Level Agent

### 6.1. Phân Tích Tensor Không Gian Trạng Thái Đầu Vào `LLNodeScorer`
* Kích thước đồ thị: $N = 50$. Vector trạng thái: $Z_{\text{nodes}} \in \mathbb{R}^{50 \times 32}$.
* Nút nguồn chặng hiện tại $\mathbf{z}_{n_{i-1}} \in \mathbb{R}^{32}$.
* Ma trận mở rộng kích thước $50 \times D$:
  * $Z_G = \mathbf{z}_{\text{global}} \otimes \mathbf{1}_{50}^{\top} \in \mathbb{R}^{50 \times 64}$
  * $Z_P = \mathbf{z}_{n_{i-1}} \otimes \mathbf{1}_{50}^{\top} \in \mathbb{R}^{50 \times 32}$
  * $V_F = \boldsymbol{\phi}(v_i) \otimes \mathbf{1}_{50}^{\top} \in \mathbb{R}^{50 \times 2}$
  * $S_F = \boldsymbol{\phi}(q_k) \otimes \mathbf{1}_{50}^{\top} \in \mathbb{R}^{50 \times 7}$
* Nối toàn bộ thông tin tạo ma trận đánh giá:
  $$X_{\text{LL}} = \left[Z_G \,\Vert{}\, Z_P \,\Vert{}\, Z_{\text{nodes}} \,\Vert{}\, V_F \,\Vert{}\, S_F\right] \in \mathbb{R}^{50 \times 137}$$

### 6.2. Vector-Masked Infeasible Action Pruning & Delayed Reward Redistribution
* Áp dụng mặt nạ nhị phân CPU: $\mathbf{m}_u = 1$ nếu `is_server=True` và $c_u^{\text{free}} \ge r_i^{\text{cpu}}$, ngược lại $=0$. Điểm số sau che mặt nạ gán $-\infty$ cho nút không hợp lệ.
* Tích lũy transition LL vào bộ nhớ tạm `ll_transitions` và phân rã phần thưởng theo 2 nhánh Thành công / Thất bại tương tự đặc tả gốc.

---

## 7. Tương Tác Giữa Các Agents Và Chu Kỳ Huấn Luyện Đồng Thời

              +----------------------------------+
              | Trạng thái Mạng G_t, Hàng đợi Q_t |
              +-----------------+----------------+
                                |
                (mỗi 20 SFC, không phải mỗi SFC)
                                v
              +----------------------------------+
              |  VGAE Forward (Neg-Sampling BCE) |
              |  + KL + GRU Update z_global      |
              +-----------------+----------------+
                                |
                z_nodes (50x32), z_global (1x64)
                                |
                                v
              +----------------------------------+
              |   HL Agent Action (Pareto-cond)  |
              |     Batch forward Q_H qua Q_t    |
              |        Chọn SFC q* (argmax)      |
              +-----------------+----------------+
                                |
                     SFC q* được chọn
                                |
                                v
 +-------------------------------------------------------------+
 | Vòng lặp Low-Level Agent (Tuần tự i = 1 ... F_k)             |
 |   1. Tính Mask CPU: m_u = (cpu_free >= r_i)                 |
 |   2. Batch forward 50 nodes qua Q_L -> Chọn n_i (argmax)    |
 |   3. Gọi custom_dijkstra(n_{i-1} -> n_i, b_k) (no stale pop)|
 |   4. Cấp phát tài nguyên & tính step cost r_L (vector hoá)  |
 +------------------------------+------------------------------+
                                |
                 Kiểm tra điều kiện kết thúc chuỗi
                                |
         +----------------------+----------------------+
         | Thành công                                  | Thất bại
         v                                             v
+---------------------------+                +---------------------------+
| - Commit + deploy cost    |                | - Rollback tài nguyên     |
| - r_a, r_c -> r_H (Cheb)  |                | - Gán r_H = -r_fail       |
| - Cập nhật Utopia points  |                | - Phạt đều buffer LL      |
+-------------+-------------+                +-------------+-------------+
|                                            |
+---------------------+----------------------+
|
v
+----------------------------------+
|       HL Replay Buffer Push      |
|   (z_global, phi(q*), w, r_H...) |
+-----------------+----------------+
|
v
+----------------------------------+
|   HL & LL Neural Network Train   |
|   - Sample Batch 64 từ 2 Buffer  |
|   - Tính Bellman MSE Target Loss |
|   - Clip Grad Norm 10.0 & Adam   |
+-----------------+----------------+
|
v
+----------------------------------+
|  Hard Update Target Networks     |
|  (Nếu train_steps % 50 == 0)     |
+----------------------------------+


---

## 8. Cấu Trúc Chi Tiết Thuật Toán Load-Aware Dijkstra

### 8.1. Hàm Trọng Số Cạnh Động & Tối Ưu
$$\rho_{uv} = \min\left(1.0 - 10^{-9}, \max\left(0.0, 1.0 - \frac{b_{uv}^{\text{free}}}{b_{uv}^{\text{total}}}\right)\right)$$
$$w(u, v) = d_{uv} + \lambda_{\text{penalty}} \cdot \left(e^{\rho_{uv} \cdot \ln \omega_{\text{bw}}} - 1.0\right)$$
với $\omega_{\text{bw}} = 100.0, \lambda_{\text{penalty}} = 1.0$. Tính mũ qua `math.exp` tối ưu. Bỏ qua các stale heap entries sau khi pop.

### 8.2. Tính Toán Reward Chặng Của Low-Level Agent
Sau khi Dijkstra xác định đường đi $P$, chi phí tức thời $r_{t,i}^L$ được tính bằng mảng vector hóa NumPy:
$$r_{t,i}^L = -\left(\alpha \cdot \text{Cost}(P) + \beta \cdot \text{Delay}(P) + \gamma_{\text{load}} \cdot \text{Std}_{\text{load}}(G)\right)$$
với $\text{Std}_{\text{load}}(G)$ lấy từ cache môi trường.

---

## 9. Cơ Chế Quản Lý Bộ Nhớ Đệm Replay Buffer

| Thuộc tính bộ đệm | `HLReplayBuffer` | `LLReplayBuffer` |
|---|---|---|
| **Dung lượng tối đa (`capacity`)** | $15,000$ mẫu | $72,000$ mẫu |
| **Kích thước một phần tử** | Thêm 2 tensor `pareto_w` (2,) so với bản gốc | 5 tensors (64, 32, 32, 2, 7) + 6 phần tử next_state + 1 float + 1 float |
| **Điều kiện bắt đầu huấn luyện** | $\text{len} \ge \min(64, 1500) = 64$ | $\text{len} \ge \min(64, 7200) = 64$ |
| **Chiến lược lấy mẫu** | Uniform Random Sampling không hoàn lại (`random.sample`) | Uniform Random Sampling không hoàn lại (`random.sample`) |
| **Vị trí lưu trữ dữ liệu** | CPU RAM (chuyển lên GPU khi gom thành Mini-batch) | CPU RAM (chuyển lên GPU khi gom thành Mini-batch) |

---

## 10. Trực Quan Hóa Kết Quả (`scripts/plot_results.py`)

* `plot_training(rows, out_dir)`: Sinh biểu đồ đường theo episode cho các chỉ số (bao gồm `total_deploy_cost`, `w_accept`, `w_cost`) và lưới tổng quan 2×4.
* `plot_pareto_front(pareto_json, out_dir)`: Đọc kết quả từ `evaluate.py --pareto-scan` (`pareto_front.json`), vẽ scatter biểu diễn biên Pareto không bị trội (non-dominated front).
* Lệnh chạy: `python scripts/plot_results.py --csv training_log.csv --pareto-json pareto_front.json --out plots`.